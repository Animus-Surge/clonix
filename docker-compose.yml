# CloNIX testing compose file for sql database container

services:
  clonix-dbtest:
    image: container-registry.oracle.com/mysql/community-server:latest
    ports:
      - "3301:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "supersecurepassword123"
      MYSQL_ROOT_HOST: "%"

